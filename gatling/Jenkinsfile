node {
    withMaven(maven:'maven') {

        stage('Checkout') {
            git url: 'https://github.com/MadeInChina/PiggyMetrics.git', credentialsId: 'github-slam', branch: 'dev'
        }

        stage('Build') {
           dir ('gatling') {
                sh 'mvn clean install'

                def pom = readMavenPom file:'pom.xml'
                print pom.version
                env.version = pom.version
            }
        }

        stage('Image') {
            dir ('gatling') {
                 def app =  docker.build "slamhan/piggymetrics-gatling:2.0.2.RELEASE"
                 app.push()
            }
        }

    }

}