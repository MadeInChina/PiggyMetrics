pipeline {
    agent any

    tools {
            maven 'maven'
    }
    stages {
        stage('Build') {
            steps {
                dir ('infrastructure-services/mongodb') {
                   sh 'mvn clean install'
                }
            }
        }
        stage('Image Build And Push') {
            steps {
                dir ('infrastructure-services/mongodb') {
                    script {
                        def app = docker.build 'slamhan/piggymetrics-mongodb:2.0.2.RELEASE'
                        app.push()
                    }
                }
            }
        }
    }
}