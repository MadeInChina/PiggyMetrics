apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: admin
  name: admin
  namespace: piggmetrics
spec:
  type: ClusterIP
  ports:
    - port: 8200
      protocol: TCP
      targetPort: 8888
  selector:
    io.kompose.service: admin
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    io.kompose.service: admin
  name: admin
  namespace: piggmetrics
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  template:
    metadata:
      labels:
        io.kompose.service: admin
    spec:
      containers:
        - env:
            - name: CONFIG_SERVICE_PASSWORD
              value: password
          image: slamhan/piggymetrics-admin:2.0.4-SNAPSHOT
          name: registry
          ports:
            - containerPort: 8080
      restartPolicy: Always