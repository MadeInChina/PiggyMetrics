hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 20000

spring:
  data:
    mongodb:
      database: piggymetrics
      port: 0

rates:
  url: http://data.fixer.io/api/latest?access_key=${FIXER_ACCESS_KEY}