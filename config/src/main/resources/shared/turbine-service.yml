turbine:
  instanceUrlSuffix:
    default: actuator/hystrix.stream
  combine-host-port: true

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream
  endpoint:
    hystrix:
      stream:
        enabled: true
management:
  endpoints:
    web:
      base-path: /
hystrix:
  metrics:
    enabled: true